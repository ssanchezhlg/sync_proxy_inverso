---
- name: Clonar repositorio en servidor local
  hosts: servers_docker
  tasks:
    - name: Clonar el repositorio
      ansible.builtin.git:
        repo: https://github.hlg.sld.cu/Nodo_Provincial/Proxy_Inverso1.git
        dest: /tmp

